# 算法库使用指南 🧮

> 数学建模常用算法的实现与应用

## 🔍 如何选择算法？

### 1️⃣ 优化问题 (Optimization)

**问题特征**: 在给定约束条件下，寻找目标函数的最优解

| 算法类型 | 适用场景 | 位置 |
|---------|---------|------|
| **遗传算法** | 离散优化、多峰函数、组合优化 | [`genetic_algorithm/`](optimization/genetic_algorithm/) |
| **模拟退火** | 全局优化、避免局部最优 | [`simulated_annealing/`](optimization/simulated_annealing/) |
| **粒子群算法** | 连续优化、多目标优化 | [`particle_swarm/`](optimization/particle_swarm/) |
| **线性规划** | 线性约束、线性目标函数 | [`linear_programming/`](optimization/linear_programming/) |

**选择建议**:
- 变量是连续的 → 粒子群算法、梯度下降
- 变量是离散的 → 遗传算法、模拟退火
- 有线性约束 → 线性规划、整数规划
- 多目标优化 → NSGA-II、粒子群

### 2️⃣ 预测问题 (Prediction)

**问题特征**: 根据历史数据预测未来趋势或未知值

| 算法类型 | 适用场景 | 位置 |
|---------|---------|------|
| **时间序列** | 时间相关数据、趋势预测 | [`time_series/`](prediction/time_series/) |
| **回归分析** | 连续值预测、因果关系 | [`regression/`](prediction/regression/) |
| **神经网络** | 复杂非线性关系、大数据 | [`neural_network/`](prediction/neural_network/) |

**选择建议**:
- 数据有时间顺序 → ARIMA、Prophet、LSTM
- 线性关系明显 → 线性回归、岭回归
- 非线性关系 → 随机森林、神经网络、SVR
- 分类预测 → 逻辑回归、SVM、决策树

### 3️⃣ 评价问题 (Evaluation)

**问题特征**: 对多个方案进行综合评价和排序

| 算法类型 | 适用场景 | 位置 |
|---------|---------|------|
| **层次分析法 (AHP)** | 主观权重、层次结构 | [`ahp/`](evaluation/ahp/) |
| **TOPSIS** | 多指标综合评价 | [`topsis/`](evaluation/topsis/) |
| **模糊综合评价** | 模糊性、不确定性 | [`fuzzy_evaluation/`](evaluation/fuzzy_evaluation/) |

**选择建议**:
- 需要专家打分 → AHP、德尔菲法
- 客观数据评价 → TOPSIS、熵权法
- 数据模糊不确定 → 模糊综合评价
- 多层次结构 → AHP、网络分析法

### 4️⃣ 图论与网络问题 (Graph & Network)

**问题特征**: 涉及节点、边、路径、流量等网络结构

| 算法类型 | 适用场景 | 位置 |
|---------|---------|------|
| **最短路径** | 路径规划、导航问题 | [`shortest_path/`](graph_network/shortest_path/) |
| **网络流** | 流量分配、运输问题 | [`network_flow/`](graph_network/network_flow/) |
| **图着色** | 资源分配、调度问题 | [`graph_coloring/`](graph_network/graph_coloring/) |

**选择建议**:
- 寻找最短路径 → Dijkstra、A*、Floyd
- 最大流问题 → Ford-Fulkerson、Dinic
- 最小生成树 → Prim、Kruskal
- 匹配问题 → 匈牙利算法、KM算法

### 5️⃣ 统计分析 (Statistics)

**问题特征**: 数据分析、假设检验、相关性分析

| 算法类型 | 适用场景 | 位置 |
|---------|---------|------|
| **假设检验** | 显著性检验、参数检验 | [`hypothesis_test/`](statistics/hypothesis_test/) |
| **相关分析** | 变量关系、相关系数 | [`correlation/`](statistics/correlation/) |
| **聚类分析** | 数据分组、模式识别 | [`clustering/`](statistics/clustering/) |

**选择建议**:
- 检验差异显著性 → t检验、方差分析
- 分析变量关系 → 相关系数、协方差
- 数据分组 → K-means、层次聚类、DBSCAN
- 降维处理 → PCA、因子分析

## 📖 算法文件夹结构

每个算法文件夹通常包含：

```
algorithm_name/
├── README.md           # 算法说明
│   ├── 算法原理
│   ├── 适用场景
│   ├── 参数说明
│   └── 使用注意事项
├── algorithm.py        # 算法实现
├── example.py          # 使用示例
└── test_data/          # 测试数据（如有）
```

## 🚀 快速使用

### 示例1: 遗传算法求解TSP问题

```python
from algorithms.optimization.genetic_algorithm import GeneticAlgorithm
import numpy as np

# 定义目标函数（旅行商问题的总距离）
def tsp_distance(route, distance_matrix):
    total_distance = 0
    for i in range(len(route) - 1):
        total_distance += distance_matrix[route[i]][route[i+1]]
    total_distance += distance_matrix[route[-1]][route[0]]
    return total_distance

# 创建遗传算法实例
ga = GeneticAlgorithm(
    pop_size=100,
    generations=500,
    mutation_rate=0.01,
    crossover_rate=0.8
)

# 求解
best_route = ga.optimize(tsp_distance, distance_matrix)
print(f"最优路径: {best_route}")
```

### 示例2: AHP层次分析法

```python
from algorithms.evaluation.ahp import AHP

# 构造判断矩阵
criteria_matrix = np.array([
    [1, 2, 5],
    [1/2, 1, 3],
    [1/5, 1/3, 1]
])

# 创建AHP实例
ahp = AHP(criteria_matrix)

# 计算权重
weights = ahp.calculate_weights()
print(f"权重向量: {weights}")

# 一致性检验
cr = ahp.consistency_ratio()
print(f"一致性比率: {cr}")
if cr < 0.1:
    print("通过一致性检验")
```

### 示例3: 时间序列预测

```python
from algorithms.prediction.time_series import ARIMA
import pandas as pd

# 加载数据
data = pd.read_csv('data/sample/time_series.csv')

# 创建ARIMA模型
model = ARIMA(order=(1, 1, 1))
model.fit(data['value'])

# 预测未来10期
forecast = model.predict(steps=10)
print(f"预测结果: {forecast}")
```

## 💡 算法选择决策树

```
问题类型？
├─ 寻找最优解
│  ├─ 变量连续 → 粒子群、梯度下降
│  ├─ 变量离散 → 遗传算法、模拟退火
│  └─ 有约束条件 → 线性规划、整数规划
│
├─ 预测未来值
│  ├─ 时间序列 → ARIMA、Prophet、LSTM
│  ├─ 线性关系 → 线性回归、岭回归
│  └─ 非线性关系 → 随机森林、神经网络
│
├─ 方案评价
│  ├─ 主观权重 → AHP、德尔菲法
│  ├─ 客观权重 → 熵权法、TOPSIS
│  └─ 模糊评价 → 模糊综合评价
│
├─ 网络问题
│  ├─ 最短路径 → Dijkstra、Floyd
│  ├─ 最大流 → Ford-Fulkerson
│  └─ 匹配问题 → 匈牙利算法
│
└─ 数据分析
   ├─ 假设检验 → t检验、卡方检验
   ├─ 相关分析 → Pearson、Spearman
   └─ 聚类分组 → K-means、层次聚类
```

## 📚 学习建议

### 初学者路线
1. **优化算法**: 遗传算法 → 模拟退火 → 粒子群
2. **预测模型**: 线性回归 → 时间序列 → 神经网络
3. **评价方法**: AHP → TOPSIS → 熵权法
4. **图论算法**: 最短路径 → 最小生成树 → 网络流

### 进阶学习
- 深入理解算法原理和数学推导
- 学习算法的改进和变种
- 掌握算法参数调优技巧
- 了解算法的适用边界和局限性

## ⚠️ 使用注意事项

1. **算法选择**: 根据问题特征选择合适的算法，不要盲目使用复杂算法
2. **参数调优**: 不同问题需要不同的参数设置，建议多次实验
3. **结果验证**: 使用多种方法验证结果的合理性
4. **计算效率**: 注意算法的时间复杂度，大规模问题考虑优化
5. **模型解释**: 能够清楚解释算法原理和结果含义

## 🔗 相关资源

- **可视化工具**: [`../visualization/`](../visualization/)
- **数据处理**: [`../utils/data_processing.py`](../utils/data_processing.py)
- **完整示例**: [`../examples/`](../examples/)
- **参考资料**: [`../reference/`](../reference/)

---

**提示**: 每个算法子文件夹都有详细的 README 说明，建议先阅读再使用！
